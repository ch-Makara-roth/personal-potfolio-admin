# Production Environment Variables
# Copy these to Vercel Dashboard → Settings → Environment Variables

# API Configuration - UPDATE WITH YOUR ACTUAL PRODUCTION API URL
NEXT_PUBLIC_API_BASE_URL=https://your-backend-api.com/api
NEXT_PUBLIC_APP_URL=https://your-portfolio-admin.vercel.app

# Authentication - GENERATE A SECURE SECRET (see instructions below)
# Run: openssl rand -base64 32
NEXTAUTH_SECRET=your-production-secret-here
NEXTAUTH_URL=https://your-portfolio-admin.vercel.app

# Instructions:
# 1. Replace the URLs above with your actual production URLs
# 2. Generate a secure NEXTAUTH_SECRET: openssl rand -base64 32
# 3. Add these to Vercel Dashboard (NOT in this file)
# 4. This file is for reference only - DO NOT commit secrets to git
